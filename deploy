#!/bin/bash
# deploy: push style.css/live.js live (GitHub Pages, ~1-2 min to propagate,
# browsers refresh within 10 min thanks to max-age=600)
set -e
cd "$(dirname "$0")"
git add -A
git commit -m "${1:-update}

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>" || true
git push -q
echo "✓ pushed — live at https://mohdshehri-dev.github.io/mohdshehri-blog-style/ within ~2 min"
